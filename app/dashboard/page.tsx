import Card from '@/components/Card';

export default function Dashboard({}: {
  profits: React.ReactNode;
  revenue: React.ReactNode;
  users: React.ReactNode;
}) {
  return <div className="flex flex-row gap-5">DashBoard</div>;
}
